<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
    <Page.actionBar>
        <ActionBar title="signalr" icon="" class="action-bar">
        </ActionBar>
    </Page.actionBar>
    <StackLayout class="p-20">
        <ScrollView class="h-full">
          <StackLayout>
            <Button text="Conn" tap="connect" class="btn btn-primary"/>
            <Button text="Dis" tap="desconectar" class="btn btn-primary"/>
            <Button text="State" tap="getStaateConn" class="btn btn-primary"/>
            <Button text="invoke" tap="invoke" class="btn btn-primary"/>
            <Button text="ons" tap="ons" class="btn btn-primary"/>

          </StackLayout>
        </ScrollView>
    </StackLayout>
</Page>